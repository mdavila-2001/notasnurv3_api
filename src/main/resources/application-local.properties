# Perfil local de ejemplo (no guardar secretos reales en este archivo).
# Define variables de entorno DB_URL, DB_USER y DB_PASSWORD para sobreescribir estos defaults.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/notasnurv3}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
